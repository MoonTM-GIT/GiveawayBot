# /ping
- name: ping
  description: Checks the Bot's Gateway Ping
  handler: dev.moontm.giveawaybot.systems.commands.PingCommand

- name: giveaway
  description: Commands that allows users create and delete Giveaways.
  subCommands:
    - name: create
      description: Create a new Giveaway in the current channel.
  handler: dev.moontm.giveawaybot.systems.giveaway.GiveawayCommandHandler
#    - name: discard
#      description: Discards a single Meeting.
#     options:
#        - name: meeting-id
#          description: The Meeting's id.
#          required: true
#          autocomplete: true
#          type: INTEGER
